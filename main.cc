#include <iostream>

#include "monster.h"

using namespace std;

int main() {
  cout << "Hello, world! :)" << endl;

  // Player p(1);
  // MeatMan m(1);
  // while (p.is_alive() && m.is_alive()) {
  //   Effect retaliation;
  //   m.apply_effect(p.create_attack(), &retaliation);
  //   p.apply_effect(*retaliation);
  // }

  return 0;
}
